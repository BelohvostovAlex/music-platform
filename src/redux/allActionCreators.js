import {
  nextSong,
  playPause,
  prevSong,
  selectGenreListId,
  setActiveSong,
} from "./features/playerSlice";

export const allActionCreators = {
  nextSong,
  playPause,
  prevSong,
  selectGenreListId,
  setActiveSong,
};
